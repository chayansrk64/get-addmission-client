 
const SectionTitle = ({heading}) => {
    return (
        <div className="text-center text-3xl py-5 underline uppercase">
            {heading}
        </div>
    );
};

export default SectionTitle;